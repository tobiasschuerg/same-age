services:
  sameage:
    build: .
    ports:
      - "5050:5000"
    environment:
      - IMMICH_URL=${IMMICH_URL:-}
      - IMMICH_API_KEY=${IMMICH_API_KEY:-}
    volumes:
      - sameage-data:/data
    restart: unless-stopped

volumes:
  sameage-data:
